[project]
name = "neurotrends"
version = "0.1.0"
description = "Static neurotrends site generator built with FastHTML."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-fasthtml>=0.12.33",
    "pandas>=2.3",
    "numpy>=2.3",
]

[tool.uv]
dev-dependencies = []
